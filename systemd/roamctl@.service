[Unit]
Description=roamctl Wi-Fi roaming daemon (%i)
After=network.target
StartLimitIntervalSec=0

[Service]
ExecStart=/usr/local/bin/roamctl -iface %i
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target